package com.tallerwebi.integracion.entrenamiento2.ejercicio1;
import static org.springframework.test.web.servlet.request.MockMvcRequestBuilders.get;
import static org.springframework.test.web.servlet.result.MockMvcResultMatchers.status;
import static org.springframework.test.web.servlet.result.MockMvcResultMatchers.view;
import org.junit.jupiter.api.Test;
import org.springframework.test.web.servlet.MockMvc;


public class ControladorContactoTest {
/*
    
    private MockMvc mockMvc;

    @Test
    void queSePuedeCargarLaVistaContactoCorrectamente() throws Exception {
        mockMvc.perform(get("/spring/contacto"))
                .andExpect(status().isOk())//valida de que no me de error 500
                .andExpect(view().name("contacto"));
    }*/
}
